server:
  port: 8080

spring:
  profiles:
    group:
      "local": "db, secret, redis"
      "prod": "db, secret, redis"
    default: local

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
