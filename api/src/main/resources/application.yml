server:
  port: 8080

spring:
  profiles:
    group:
      "local": "db, secret, redis, security, s3"
      "prod": "db, secret, redis, security, s3"
    default: local

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
